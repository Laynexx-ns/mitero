# Mitero

A simple file observer that performs specified actions on file system events. Plugin support and Docker integration are planned.


To run this project use latest bun version:

```bash
bun i
bun src/bootstrap
```
